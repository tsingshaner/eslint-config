---
"@qingshaner/eslint-config": patch
---

- Normalize Changesets `config.json` (key ordering, `ignore` as an array).
- Extend exported `cspell` preset `ignorePaths` for npm/pnpm/yarn/bun lockfiles.
